# Prompt Manifest

Prompt text files are version-controlled here.
Each prompt gets a `PromptVersion` row in the database with content hash and active flag.

## Naming convention
- `{prompt-name}/v{version}.txt`
- Example: `task-dedup/v1.txt`, `task-dedup/v2.txt`
