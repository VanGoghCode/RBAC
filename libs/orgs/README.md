# orgs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orgs` to build the library.

## Running unit tests

Run `nx test orgs` to execute the unit tests via [Jest](https://jestjs.io).
